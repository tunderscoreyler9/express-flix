<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TMDb Movie Search</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1>Search for a Movie</h1>
    <form action="/" method="GET">
        <input type="text" name="query" placeholder="Enter movie title">
        <button type="submit">Search</button>
    </form>

    <% if (results.length > 0) { %>
        <h2>Results for "<%= query %>"</h2>
        <ul>
            <% results.forEach(movie => { %>
                <li>
                    <h3><%= movie.title %></h3>
                    <p>Release Date: <%= movie.release_date %></p>
                    <p><%= movie.overview %></p>
                    <% if (movie.poster_path) { %>
                        <img src="https://image.tmdb.org/t/p/w185<%= movie.poster_path %>" alt="<%= movie.title %> Poster" style="max-width: 200px; max-height: 300px;">
                    <% } else { %>
                        <p>No poster available</p>
                    <% } %>
                </li>
            <% }) %>
        </ul>
    <% } else if (query) { %>
        <p>No results found for "<%= query %>"</p>
    <% } %>
</body>
</html> -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TMDb Movie Search</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @keyframes spin {
            to { transform: rotate(360deg); }
        }
        .animate-spin-slow {
            animation: spin 1.5s linear infinite;
        }
    </style>
</head>
<body class="bg-gray-100 min-h-screen">
    <div class="container mx-auto px-4 py-8">
        <!-- Header -->
        <header class="text-center mb-8">
            <h1 class="text-4xl font-bold text-gray-800 mb-4">Movie Search</h1>
            <form id="searchForm" action="/" method="GET" class="max-w-xl mx-auto">
                <div class="flex gap-2">
                    <input 
                        type="text" 
                        name="query" 
                        placeholder="Enter movie title..." 
                        class="flex-1 px-4 py-2 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500"
                        value="<%= query %>"
                    >
                    <button 
                        type="submit" 
                        id="searchButton"
                        class="px-6 py-2 bg-blue-500 text-white rounded-lg hover:bg-blue-600 transition-colors flex items-center gap-2"
                    >
                        <span>Search</span>
                    </button>
                </div>
            </form>
        </header>

        <!-- Loading State -->
        <div id="loadingState" class="hidden">
            <div class="flex flex-col items-center justify-center py-12">
                <svg class="animate-spin-slow w-16 h-16 text-blue-500" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                    <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                    <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
                </svg>
                <p class="mt-4 text-lg text-gray-600">Searching for movies...</p>
            </div>
        </div>

        <!-- Results Section -->
        <div id="resultsSection">
            <% if (results.length > 0) { %>
                <h2 class="text-2xl font-semibold text-gray-700 mb-6">Results for "<%= query %>"</h2>
                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                    <% results.forEach(movie => { %>
                        <div class="bg-white rounded-xl shadow-md overflow-hidden hover:shadow-lg transition-shadow">
                            <div class="aspect-w-2 aspect-h-3 relative">
                                <% if (movie.poster_path) { %>
                                    <img 
                                        src="https://image.tmdb.org/t/p/w500<%= movie.poster_path %>" 
                                        alt="<%= movie.title %> Poster"
                                        class="w-full h-64 object-cover"
                                    >
                                <% } else { %>
                                    <div class="w-full h-64 bg-gray-200 flex items-center justify-center">
                                        <span class="text-gray-400">No poster available</span>
                                    </div>
                                <% } %>
                            </div>
                            <div class="p-4">
                                <h3 class="text-xl font-semibold text-gray-800 mb-2"><%= movie.title %></h3>
                                <p class="text-sm text-gray-500 mb-3">Release Date: <%= movie.release_date %></p>
                                <p class="text-gray-600 text-sm line-clamp-3"><%= movie.overview %></p>
                            </div>
                        </div>
                    <% }) %>
                </div>
            <% } else if (query) { %>
                <div class="text-center py-12">
                    <p class="text-lg text-gray-600">No results found for "<%= query %>"</p>
                </div>
            <% } %>
        </div>
    </div>

    <!-- Load JavaScript at the end of the body -->
    <script src="/js/search.js"></script>
</body>
</html>