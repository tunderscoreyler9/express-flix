<nav class="bg-blue-600 shadow-md">
    <div class="container mx-auto px-4 py-3 flex flex-col md:flex-row md:items-center md:justify-between">
        <!-- Logo & Title -->
        <div class="flex items-center justify-between">
            <a href="/" class="flex items-center text-white">
                <svg class="w-8 h-8 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" 
                        d="M7 4v16M17 4v16M3 8h4m10 0h4M3 12h18M3 16h4m10 0h4M4 20h16a1 1 0 001-1V5a1 1 0 00-1-1H4a1 1 0 00-1 1v14a1 1 0 001 1z" />
                </svg>
                <span class="text-xl font-bold">MovieFinder</span>
            </a>
        </div>

        <!-- Search Form -->
        <div class="mt-4 md:mt-0 flex-grow md:max-w-xl mx-auto">
            <form id="searchForm" action="/" method="GET" class="flex">
                <div class="flex-grow mr-2 relative">
                    <input 
                        type="text" 
                        id="searchInput" 
                        name="query" 
                        placeholder="Search movies or actors..." 
                        value="<%= typeof query !== 'undefined' ? query : '' %>"
                        class="w-full px-4 py-2 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500">
                </div>
                <!-- Search type selector -->
                <div class="mr-2">
                    <select name="searchType" class="h-full px-3 py-2 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500">
                        <option value="movie" <%= typeof searchType !== 'undefined' && searchType === 'movie' ? 'selected' : '' %>>Movies</option>
                        <option value="actor" <%= typeof searchType !== 'undefined' && searchType === 'actor' ? 'selected' : '' %>>Actors</option>
                    </select>
                </div>
                <button 
                    id="searchButton"
                    type="submit"
                    class="bg-white text-blue-600 px-4 py-2 rounded-lg font-semibold hover:bg-blue-50 focus:outline-none focus:ring-2 focus:ring-white">
                    Search
                </button>
            </form>
        </div>

        <!-- Navigation Links -->
        <div class="mt-4 md:mt-0 flex">
            <a href="/" class="text-white hover:text-blue-200 px-3 py-2">
                Home
            </a>
            <a href="/?category=trending" class="text-white hover:text-blue-200 px-3 py-2">
                Trending
            </a>
            <a href="/?category=top_rated" class="text-white hover:text-blue-200 px-3 py-2">
                Top Rated
            </a>
        </div>
    </div>
</nav>